input {
  lumberjack {
    # The port to listen on
    port => 33334

    # The paths to your ssl cert and key
    ssl_certificate => "/etc/puppet/files/logstash-forwarder.crt"
    ssl_key => "/etc/puppet/files/logstash-forwarder.key"

    # Set this to whatever you want.
    type => "nginx"
  }
}